FROM nginx:alpine

# Timpa default config dengan app.conf Anda
COPY app.conf /etc/nginx/conf.d/default.conf

# Nginx harus memiliki struktur folder yang sama dengan container 'app'
WORKDIR /var/www
COPY public /var/www/public
